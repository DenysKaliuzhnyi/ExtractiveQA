version: "3.9"
services:
  fastapi:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - .:/app
  bot:
    build: .
    command: python3 bot.py
    depends_on:
      - fastapi
    volumes:
      - ./secrets/extractiveqa-service-accout-key.json:/app/key.json
    environment:
      - API_URL=http://fastapi:8080
      - GOOGLE_APPLICATION_CREDENTIALS=/app/key.json
      - GCP_PROJECT_ID=extractiveqa-442314